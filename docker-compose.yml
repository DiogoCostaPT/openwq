version: '3.3'
services:
  openwq_summa:
    build: ./
    tty: true
    container_name: openWQ
    volumes:
      - ./:/code:Z
      - /u1/kck540/SummaActorsSettings/./:/SUMMA:Z # Can be removed
      - /u1/kck540/forcingData/./:/forcing_data:Z  # Can be removed
